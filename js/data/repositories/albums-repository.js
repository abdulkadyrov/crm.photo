import { createRepository } from "./base-repository.js";

export const albumProjectsRepository = createRepository("albumProjects");
export const albumClassesRepository = createRepository("albumClasses");
export const albumStudentsRepository = createRepository("albumStudents");
export const albumMediaRepository = createRepository("albumMedia");
export const albumGroupMediaRepository = createRepository("albumGroupMedia");
export const albumTeachersRepository = createRepository("albumTeachers");

